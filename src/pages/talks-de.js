import React from "react"
import TalksCatalog from "../components/TalksCatalog"

const TalksDePage = ({ location }) => (
 <TalksCatalog locale="de" location={location} />
)

export default TalksDePage
